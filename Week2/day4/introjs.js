// In JavaScript, the indexOf() method takes a string and a substring and returns the starting index of the first occurrence of the given substring in that string.

let name = "Seem The Damn Cliffe"
let dSpot = name.indexOf("D")

name[dSpot] = "F"
console.log(dSpot)

console.log(name.indexOf("e"))


// The split() method in JavaScript enables us to break down a string into an array of (smaller) strings. Let’s jump directly into the code.

// 2 ways to run js code

